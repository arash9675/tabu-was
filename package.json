{
  "name": "tabu-wash",
  "version": "1.0.0",
  "description": "Tabu Wash — Capacitor Android wrapper (MVP)",
  "private": true,
  "scripts": {
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
